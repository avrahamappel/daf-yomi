{
  "name": "frontend",
  "version": "0.1.0",
  "scripts": {
    "build:wasm": "wasm-pack build --target web --out-dir ./pkg",
    "dev": "vite"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
